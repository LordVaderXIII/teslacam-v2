version: '3.8'

services:
  backend:
    image: lordvaderxiii/teslacam-v2-backend:latest
    env_file:
      - .env
    ports:
      - "3001:3001"
    volumes:
      - ${TESLACAM_DIR:-./teslacam}:/usr/src/app/teslacam
    user: "root"
    networks:
      - teslacam-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/api/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  frontend:
    image: lordvaderxiii/teslacam-v2-frontend:latest
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - teslacam-net

networks:
  teslacam-net:
    driver: bridge
